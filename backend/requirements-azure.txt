# Optimized requirements for Azure App Service F1 tier
# Reduced dependencies for memory efficiency and faster cold starts

# Core FastAPI dependencies
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Database - SQLite for cost optimization, PostgreSQL optional
sqlalchemy==2.0.35
# psycopg2-binary==2.9.10  # Uncomment if using PostgreSQL
alembic==1.14.0  # Keep for database migrations

# Authentication
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.17

# AI/LLM - Updated to resolve conflicts
groq==0.9.0
langchain-core==0.3.64
langchain==0.3.14
langchain-groq==0.2.2
langchain-community==0.3.14

# Email validation (required for user registration)
email-validator==2.1.1

# File handling - minimal set
python-docx==1.1.2
jinja2==3.1.4

# HTTP client - async for efficiency
httpx==0.27.0

# File handling - PDF support
pypdf==5.1.0

# Redis (optional for caching - can be disabled in production)
redis==5.2.0

# Azure specific dependencies
azure-storage-blob==12.19.0
azure-identity==1.15.0

# Logging and monitoring
structlog==23.2.0

# Security enhancements - fixed compatibility
cryptography>=42.0.0,<44.0.0
bcrypt==4.1.2
pyasn1>=0.4.6,<0.5.0

# Performance optimization
orjson==3.9.10  # Faster JSON serialization

# Testing dependencies for CI/CD
pytest==8.3.4
pytest-asyncio==0.25.0